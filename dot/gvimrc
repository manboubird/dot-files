if has("gui_running")
  set background=dark
  colorscheme solarized
  if has('gui_macvim')
    set guifont=Osaka-Mono:h12
    set lines=60 columns=200
    set transparency=3
    set guioptions-=T
  endif
endif
