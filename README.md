Personal dot files

## Installation

    /usr/bin/curl -fksSL https://raw.github.com/manboubird/dot-files/master/clone_and_link.sh | /bin/bash
    cd ~/.dot-files && git submodule init && git submodule update

## Zsh Setup

For a full zsh environment setup guide (symlinks, local config templates, required tools):

→ [docs/setup/zshrc.md](docs/setup/zshrc.md)
